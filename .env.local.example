# 复制为 .env.local 后填写。不要把 .env.local 提交到 git。
#
# Azure 一次性配置：
# 1. https://portal.azure.com → 应用注册 → 打开 writeNow
# 2. 身份验证 → 单页应用，重定向 URI 需要包含：
#    开发：http://localhost:5173/
#    上线：https://kouzomi.github.io/writeNow/
# 3. API 权限：Microsoft Graph 委派权限 Files.ReadWrite.AppFolder
# 4. 把「应用程序(客户端) ID」填到 VITE_ONEDRIVE_CLIENT_ID
#
# 生产构建使用仓库里的 .env.production，不必在本地填生产地址。

VITE_ONEDRIVE_CLIENT_ID=
VITE_ONEDRIVE_REDIRECT_URI=http://localhost:5173/
